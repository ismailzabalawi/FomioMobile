// Re-export useBottomSheetAnimatedPosition for easier external use
// This provides a cleaner import path for components that need animated position

export { useBottomSheetAnimatedPosition as useSheetAnimatedPosition } from '../bottom-sheet-animations';

